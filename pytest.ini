[pytest]
pythonpath = .

testpaths = work/tests

markers =
    unit: unit tests
    integration: integration tests

filterwarnings =
    ignore:Dropped .* row\(s\) with missing required values:UserWarning
